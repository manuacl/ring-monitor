import QtQuick
import org.kde.kirigami as Kirigami

// Standalone counterpart of platforms/plasma/ThemedIcon.qml. Kirigami
// is shipped as a runtime dep of the standalone build, so the
// adapter is the same one-liner as the Plasma side.
Kirigami.Icon {}
